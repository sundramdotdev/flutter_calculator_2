import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color background = Color(0xFF111111);
  static const Color surface = Color(0xFF1B1B1B);
  static const Color cards = Color(0xFF242424);
  
  static const Color primaryAccent = Color(0xFFC47B3A);
  
  // Text Colors
  static const Color primaryText = Color(0xFFF5F5F5);
  static const Color secondaryText = Color(0xFF9E9E9E);
  
  // Status Colors
  static const Color success = Color(0xFF4D8B5B);
  static const Color error = Color(0xFFB55252);
  
  // Light Theme Colors (Inverse logic for light theme if needed, or keeping it strictly dark as requested, but user mentioned "Create light and dark themes")
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color lightSurface = Color(0xFFE0E0E0);
  static const Color lightCards = Color(0xFFFFFFFF);
  static const Color lightPrimaryText = Color(0xFF111111);
  static const Color lightSecondaryText = Color(0xFF616161);
}
